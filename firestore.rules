rules_version = '2';

// All reads and writes go through the Next.js server with the Admin SDK, which bypasses these
// rules, so browsers get no direct access at all.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
